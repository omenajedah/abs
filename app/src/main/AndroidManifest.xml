<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thessa.absensi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.thessa.absensi.MyAplications"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.thessa.absensi.activity.login.LoginActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.thessa.absensi.activity.register.RegisterActivity"
            android:label="Register"
            android:launchMode="singleTop" />

        <activity
            android:name="com.thessa.absensi.activity.home.HomeActivity"
            android:label="Home"
            android:launchMode="singleTop" />

        <activity
            android:name="com.thessa.absensi.activity.kelas.KelasActivity"
            android:label="Kelas"
            android:launchMode="singleTop" />

        <activity
            android:name="com.thessa.absensi.activity.siswa.SiswaActivity"
            android:label="Siswa"
            android:launchMode="singleTop" />

        <activity
            android:name="com.thessa.absensi.activity.absensi.AbsensiActivity"
            android:label="Absensi"
            android:launchMode="singleTop" />

        <activity
            android:name="com.thessa.absensi.activity.laporan.LaporanActivity"
            android:label="Laporan Absensi"
            android:launchMode="singleTop" />
        <activity android:name=".activity.laporandetil.LaporanDetailActivity"
            android:label="Laporan Absensi"
            android:launchMode="singleTop"/>
    </application>

</manifest>